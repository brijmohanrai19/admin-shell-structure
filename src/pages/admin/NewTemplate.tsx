/**
 * TODO: New Template Page
 *
 * Responsibilities:
 * - Create new landing page template
 * - Define template structure with sections
 * - Configure component definitions for each section
 * - Set template metadata (name, description, version)
 * - Save as draft or publish template
 *
 * API:
 * - POST /admin/templates (create template)
 * - POST /admin/templates/:id/publish (publish template)
 *
 * Components:
 * - PageHeader
 * - TemplateEditor (template structure editor)
 * - ComponentDefinitionEditor (section config editor)
 */

export default function NewTemplate() {
  return (
    <div>
      <h1>Create New Template</h1>
      <p>TODO: Implement template creation form</p>
    </div>
  );
}
