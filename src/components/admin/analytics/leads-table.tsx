/**
 * TODO: Leads Table Component
 *
 * Responsibilities:
 * - Display lead submissions in table format
 * - Show columns: Name, Email, Phone, Source, Date, Status
 * - Sortable and filterable columns
 * - Pagination support
 * - Export leads to CSV
 * - Row actions (view details, mark as contacted)
 *
 * Props:
 * - leads: Lead[]
 * - onExport?: () => void
 */

export function LeadsTable() {
  return (
    <div>
      <p>TODO: Leads Table</p>
    </div>
  );
}
