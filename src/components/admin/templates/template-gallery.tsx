/**
 * TODO: Template Gallery Component
 *
 * Responsibilities:
 * - Display templates in grid layout
 * - Show template cards with preview thumbnails
 * - Filter templates by category
 * - Handle template selection
 *
 * Props:
 * - templates: Template[]
 * - onSelect?: (templateId: string) => void
 * - selectable?: boolean
 */

export function TemplateGallery() {
  return (
    <div>
      <p>TODO: Template Gallery</p>
    </div>
  );
}
