/**
 * TODO: Component Definition Editor
 *
 * Responsibilities:
 * - Edit component_definition for template sections
 * - Configure field definitions (type, label, default, required)
 * - Support different field types (text, textarea, image, url, etc.)
 * - Add/remove fields dynamically
 * - JSON schema validation
 *
 * Props:
 * - componentDefinition: ComponentDefinition
 * - onChange: (definition: ComponentDefinition) => void
 */

export function ComponentDefinitionEditor() {
  return (
    <div>
      <p>TODO: Component Definition Editor</p>
    </div>
  );
}
