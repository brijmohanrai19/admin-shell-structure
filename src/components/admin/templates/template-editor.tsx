/**
 * TODO: Template Editor Component
 *
 * Responsibilities:
 * - Edit template structure (sections array)
 * - Add/remove/reorder sections
 * - Configure section types (hero, form, content, etc.)
 * - Edit template metadata (name, description, version)
 * - Show locked indicator if template is published
 *
 * Props:
 * - template: Template
 * - onChange: (template: Template) => void
 * - locked?: boolean
 */

export function TemplateEditor() {
  return (
    <div>
      <p>TODO: Template Editor</p>
    </div>
  );
}
