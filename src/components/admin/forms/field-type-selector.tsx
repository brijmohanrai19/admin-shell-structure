/**
 * TODO: Field Type Selector Component
 *
 * Responsibilities:
 * - Dropdown for selecting form field type
 * - Support field types: text, email, phone, select, checkbox, textarea, date, number
 * - Display field type icon and description
 * - Update field configuration based on type
 *
 * Props:
 * - value: FieldType
 * - onChange: (type: FieldType) => void
 */

export function FieldTypeSelector() {
  return (
    <div>
      <p>TODO: Field Type Selector</p>
    </div>
  );
}
