/**
 * TODO: Form Schema Editor Component
 *
 * Responsibilities:
 * - Create/edit form schema
 * - Manage form fields (add, remove, reorder)
 * - Configure schema metadata (name, version)
 * - Show locked indicator if schema is in use
 * - Set as system default option
 *
 * Props:
 * - schema: FormSchema
 * - onChange: (schema: FormSchema) => void
 * - locked?: boolean
 */

export function FormSchemaEditor() {
  return (
    <div>
      <p>TODO: Form Schema Editor</p>
    </div>
  );
}
