/**
 * TODO: Form Preview Component
 *
 * Responsibilities:
 * - Live preview of form based on schema
 * - Render all fields with proper types and validation
 * - Show field labels, placeholders, and validation rules
 * - Read-only preview mode (no actual submission)
 * - Display form layout as it will appear to users
 *
 * Props:
 * - schema: FormSchema
 */

export function FormPreview() {
  return (
    <div>
      <p>TODO: Form Preview</p>
    </div>
  );
}
