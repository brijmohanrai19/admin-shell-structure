/**
 * TODO: Campaign Table Component
 *
 * Responsibilities:
 * - Display list of campaigns in table format
 * - Show columns: Name, URL, Status, Template, Entity Link, Created Date
 * - Filter by status (draft, live, paused, archived)
 * - Search by name or slug
 * - Actions: View, Edit (draft only), Launch, Pause, Archive
 * - Link to analytics page
 *
 * Props:
 * - campaigns: Campaign[]
 * - onStatusChange: (id: string, status: string) => void
 */

export function CampaignTable() {
  return (
    <div>
      <p>TODO: Campaign Table</p>
    </div>
  );
}
