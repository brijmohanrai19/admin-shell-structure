/**
 * TODO: Step 4 - SEO & Crawl Component
 *
 * Responsibilities:
 * - SEO override checkbox (default: use entity SEO)
 * - SEO editor (if override enabled)
 * - Crawl policy editor (default: noindex for ads)
 * - Form schema selector with resolution chain display
 * - Show default values from entity/system
 *
 * Resolution Chain Display:
 * - Campaign → Entity (Exam/College/Scholarship) → System Default
 *
 * Child Components:
 * - SEOEditor (meta title, description, keywords)
 * - CrawlPolicyEditor (indexable, follow_links, sitemap settings)
 * - FormSchemaSelector (dropdown with resolution preview)
 */

export function Step4SEO() {
  return (
    <div>
      <p>TODO: Step 4 - SEO & Crawl</p>
    </div>
  );
}
