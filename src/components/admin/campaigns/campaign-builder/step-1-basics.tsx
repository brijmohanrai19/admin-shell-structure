/**
 * TODO: Step 1 - Basics Component
 *
 * Responsibilities:
 * - Campaign name input (internal)
 * - Description textarea (internal notes)
 * - Prefix selector (ad, lp, cmp, etc.)
 * - Slug input with real-time availability check
 * - Entity selector (Exam/College/Scholarship - optional)
 *
 * API:
 * - POST /admin/slug-registry/check (debounced slug validation)
 *
 * Child Components:
 * - SlugInput (with availability indicator)
 * - EntitySelector (dropdown for exam/college/scholarship)
 */

export function Step1Basics() {
  return (
    <div>
      <p>TODO: Step 1 - Basics</p>
    </div>
  );
}
