/**
 * TODO: Campaign Status Badge Component
 *
 * Responsibilities:
 * - Display campaign status badge with appropriate color
 * - Status values: draft (gray), live (green), paused (yellow), archived (red)
 * - Consistent styling with existing StatusBadge component
 *
 * Props:
 * - status: 'draft' | 'live' | 'paused' | 'archived'
 * - className?: string
 */

export function CampaignStatusBadge() {
  return (
    <div>
      <p>TODO: Campaign Status Badge</p>
    </div>
  );
}
