/**
 * TODO: Tracker Form Component
 *
 * Responsibilities:
 * - Create/edit tracker configuration
 * - Tracker name input
 * - Script code editor (code textarea)
 * - Load position selector (head, body_start, body_end)
 * - Tracker scope selector (global, entity-level)
 * - Validation for script code
 *
 * Props:
 * - tracker?: Tracker (for editing)
 * - onSubmit: (tracker: Tracker) => void
 */

export function TrackerForm() {
  return (
    <div>
      <p>TODO: Tracker Form</p>
    </div>
  );
}
