/**
 * TODO: Tracker Resolution Preview Component
 *
 * Responsibilities:
 * - Visualize tracker resolution hierarchy
 * - Show precedence: Page > Campaign > Entity > Global
 * - Display which trackers will be injected
 * - Show deduplication by tracker_id
 * - Highlight overridden trackers
 * - Color-coded hierarchy levels
 *
 * Props:
 * - trackers: ResolvedTracker[] (with source level)
 */

export function TrackerResolutionPreview() {
  return (
    <div>
      <p>TODO: Tracker Resolution Preview</p>
    </div>
  );
}
