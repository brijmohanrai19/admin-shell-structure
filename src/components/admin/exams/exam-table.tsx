/**
 * TODO: Exam Table Component
 *
 * Responsibilities:
 * - Display exams in table format
 * - Show columns: Name, Slug, Status, Exam Date, Created Date
 * - Filter by status (draft, live, closed)
 * - Search by name or slug
 * - Actions: View, Edit, Publish, Close, Archive
 * - Link to exam ROI analytics
 *
 * Props:
 * - exams: Exam[]
 * - onStatusChange: (id: string, status: string) => void
 */

export function ExamTable() {
  return (
    <div>
      <p>TODO: Exam Table</p>
    </div>
  );
}
