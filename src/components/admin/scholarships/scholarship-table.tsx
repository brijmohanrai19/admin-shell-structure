/**
 * TODO: Scholarship Table Component
 *
 * Responsibilities:
 * - Display scholarships in table format
 * - Show columns: Name, Slug, Amount, Deadline, Status, Created Date
 * - Filter by status (draft, live, archived)
 * - Search by name or slug
 * - Actions: View, Edit, Publish, Archive
 *
 * Props:
 * - scholarships: Scholarship[]
 * - onStatusChange: (id: string, status: string) => void
 */

export function ScholarshipTable() {
  return (
    <div>
      <p>TODO: Scholarship Table</p>
    </div>
  );
}
